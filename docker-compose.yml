version: '3'

services:
  db:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123@pass
  web:
    build: .
    command: >
      bash -c "python src/credit_engine/manage.py makemigrations &&
      python src/credit_engine/manage.py migrate &&
      python src/credit_engine/manage.py populate_data src/credit_engine/data/customer_data.csv src/credit_engine/data/loan_data.csv &&
      python src/credit_engine/manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db